ssh to energon by setting X11 flag.

start the server by issuing the following command:
java Dealer <port> <mode> <player-count> <num-rounds> <num-classes> <filepath>
example: java Dealer 8080 2 1 200 16 ../gambles_n200_p80&

start the client by issuing the following command:
python qp_markowitz.py <host> <port> <mode> <gambles> <num-classes> <name-player>
example: python qp_markowitz.py localhost 8080 2 200 16 ReissSofaerMehra
